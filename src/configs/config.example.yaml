app:
  name:
  port:

db:
  host:
  username:
  password:
  database:

redis:
  host:
  port:
  user:
  pass:

cors:
  origins:
    - 'http://127.0.0.1:8000'

jwt:
  cookieName:
  verify:
    issuer:
    audience:

swagger:
  enable: true

microservice:
  server:
    transport:
    options:
      servers:
  clients:
    ucenter:
      transport:
      options:
        servers:
